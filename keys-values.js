const pen = {
  brand: 'matador',
  inkColor: 'black',
  bodyColor: 'pink',
  type: 'ball pen',
  price: 10
}

const keys = Object.keys(pen);
console.log(keys);

const values = Object.values(pen);

console.log(values);